% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipes.r
\name{backward_single_pipe}
\alias{backward_single_pipe}
\title{Apply Backward Transformations to Single Pipe}
\usage{
backward_single_pipe(pipe, env = parent.frame(), enclos = parent.frame())
}
\arguments{
\item{pipe}{A pipe parsed by \code{parse_single_pipe()}}

\item{env}{Environment where the pipe will be evaluated}

\item{enclos}{Enclosing environment for evaluation of the closures}
}
\value{
Result of applying the backward closures (in reverse order) to the
data in \code{"on"}
}
\description{
Extracts backward closures from a parsed pipe, reverses their order, and
applies them sequentially using \code{eval_single_pipe()}. This enables
inverse transformations for reversible pipelines.
}
\details{
This function applies transformations in reverse by:
\enumerate{
\item Extracting the \code{$backward} component from each
\code{shapeshiftr_closure} object
\item Reversing the order of closures with \code{rev()}
\item Delegating to \code{eval_single_pipe()} for execution
}

The reversal is critical for correct inverse transformations. If the forward
pipe applies transformations \code{f1}, then \code{f2}, then \code{f3}, the
backward pipe applies \code{f3_inv}, then \code{f2_inv}, then \code{f1_inv}.

For generators that don't provide explicit backward closures, the identity
function \code{function(x) x} is used by default.
}
\examples{
\dontrun{
library(data.table)
data(simple_dt_date)

gen_log_exp <- function(...) {
    list(
        forward = function(x) log(x),
        backward = function(x) exp(x)
    )
}

data_list <- list(data = abs(simple_dt_date$X1) + 1)
raw_pipe <- list(
    on = "data",
    transforms = list(list(fun = "gen_log_exp"))
)

parsed <- parse_single_pipe(raw_pipe, env = data_list)
forward_result <- forward_single_pipe(parsed, env = data_list)
backward_result <- backward_single_pipe(
    parsed,
    env = list(data = forward_result)
)
}

}
\seealso{
\itemize{
\item \code{\link{forward_single_pipe}} for applying forward transformations
\item \code{\link{backward_pipes}} for evaluating multiple pipes
\item \code{\link{parse_single_pipe}} for creating parsed pipes
}
}
